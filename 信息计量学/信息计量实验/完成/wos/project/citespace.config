# This is an automatically generated file. You may edit it as a text file
# Lines starting with # are comment lines.
#
# HOME: The default data directory
#
# JDIC: Function: If on, invoke Internet Explorer for Google Scholar, ACM DL, etc.
#	Usage: Edit JDIC=on/off in citespace.config.
#
# ExportSpace:
#	Function: If on, generate a file citespace.space and enable
#		search from Citation History. Required for Cluster Labeling.
#	Usage: Edit ExportSpace=on/off in citespace.config.
#
# ExportAbstract:
#	Function: If on, include abstracts in citespace.space. Required
#		for Cluster Labeling with Abstracts. Note - time consuming due to.
#		the tagging process.
#	Usage: Edit ExportAbstract=on/off in citespace.config.
#
# ExportMatrix:
#	Function: If on, export matrices.
#	Usage: Edit ExportMatrices=on/off in citespace.config.
#
# ExclusionList:
#	Function: If on, CiteSpace will exclude items listed in citespace.exclusion
#	Usage: Add entries to the list in Popup Menu | Add to the Exclusion List
#
# AliasList:
#	Format: Primary#Secondary 
#	Function: CiteSpace will repace all occurrences of Secondary with Primary
#	Usage: Use popup menu to select the primary and secondary nodes, then re-run GO!
#
# NounPhraseWordsMin:
#	Example:
#	NounPhraseWordsMin=1
#	The minimum number of words in noun phrases is 1.
#
# NounPhraseWordsMax:
#	The maximum number of words in noun phrases.
#
# SaveMergedSliceFiles:
#	Default=off, not save the files; on: save files, which can be bulky
#
# BurstWeight:
#	Default=0.0
#
# MaximumGMLNodeLabelLength:
#	Default=7
# CTSA:
#	1=Disciplines, 2=Sciences
# Group Author:
#	Default=off, not include the Group Author field
# Editors:
#	Default=off, not include the Editor field
# Node Degree Weighted:
#	Default=true
# Look Back Time:
#	How far back to retain references in the past. Default=-1, unlimited
# Normalize Citations:
#	Needs a local csv file citation_average_by_py.csv to use this function
# MaxLinksToRetain:
#	Default=5; Use -1 to retain all links
# MaxLinksPerNode:
#	Values: 1=on, -1=off
# Percentage Nodes to Label Initially:
#	1.0%
# LinkWeightModificationRate:
#	0.00000001
# minimumThresholdValue:
#	1.0
#	FilterByIntrinsicCitations:
#	on
# Concept Tree Home:
#	/Users/zhao/.citespace
####################################################
Description=How did you create the dataset?
AliasList=on
ExclusionList=on
ExportSpace=off
ExportAbstract=on
ExportMatrix=off
JDIC=on
SaveMergedSliceFiles=off
NounPhraseWordsMin=2
NounPhraseWordsMax=4
BurstWeight=0.00
MaximumGMLNodeLabelLength=8
CTSA=1
AuthorGP=off
AuthorED=off
NodeDegreeWeighted=true
LookBackTime=5
NormalizeCitations=off
GlobalCheck=off
MaxLinksToRetain=3
MaxLinksPerNode=10
PercentageNodesToLabel=1.0
LinkWeightModificationRate=0.00000000001
minimumThresholdValue=1.0
FilterByIntrinsicCitations=on
Concept Tree Home=/Users/zhao/.citespace
UseAuthorFullname=on
UseC3=on
DimensionsEndpoint=https://app.dimensions.ai/